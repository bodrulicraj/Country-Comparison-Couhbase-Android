<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.couchbase.countrycomparison">

    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name="com.countrycomparison.profile.CreateActivity"
            android:exported="false" />
        <activity
            android:name="com.countrycomparison.profile.ReadActivity"
            android:exported="false" />
        <activity
            android:name="com.countrycomparison.profile.EinputActivity"
            android:exported="false" />
        <activity
            android:name="com.countrycomparison.profile.DeleteActivity"
            android:exported="false" />
        <activity
            android:name="com.countrycomparison.login.LoginActivity"
            android:label="@string/app_name"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="com.countrycomparison.profile.UserProfileActivity" />
    </application>

</manifest>